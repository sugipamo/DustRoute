# Promoted Minecraft traces

This directory contains reviewed Minecraft Java traces promoted from ignored
`.local/e2e-artifacts` output. Every `*.trace.json` must have a matching
`*.meta.json`. Promotion is explicit and refuses to overwrite an existing
fixture.
